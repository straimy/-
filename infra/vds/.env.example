# Public DNS name pointed to the VDS. Caddy will obtain HTTPS automatically.
GGO_HOST=updates.example.com

# The same public URL, used by publish_release.py to generate manifest download URLs.
GGO_PUBLIC_BASE_URL=https://updates.example.com
