name: GGO UI Shell Stage 7 Compile

on:
  workflow_dispatch:
  push:
    paths:
      - 'hotfix/apply_ggo_ui_shell_stage1.py'
      - 'hotfix/apply_ggo_ui_shell_stage2.py'
      - 'hotfix/apply_ggo_ui_shell_stage3.py'
      - 'hotfix/apply_ggo_hud_stage4.py'
      - 'hotfix/apply_ggo_frontend_stage5.py'
      - 'hotfix/apply_ggo_settings_stage6.py'
      - 'hotfix/apply_ggo_identity_minimap_stage7.py'
      - '.github/workflows/ggo-ui-shell-stage7-compile.yml'

permissions:
  contents: read

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      SOURCE_ZIP: gunner-arena-forge-1.20.1-0.6.0-test52-GREPFIX-SOURCE.zip
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Unpack real Runtime v1 source
        shell: bash
        run: |
          set -euo pipefail
          test -f "$SOURCE_ZIP"
          rm -rf ga-build
          mkdir ga-build
          unzip -q "$SOURCE_ZIP" -d ga-build

      - name: Apply complete GGO UI patch chain
        shell: bash
        run: |
          set -euo pipefail
          python3 hotfix/apply_ggo_ui_shell_stage1.py
          python3 hotfix/apply_ggo_ui_shell_stage2.py
          python3 hotfix/apply_ggo_ui_shell_stage3.py
          python3 hotfix/apply_ggo_hud_stage4.py
          python3 hotfix/apply_ggo_frontend_stage5.py
          python3 hotfix/apply_ggo_settings_stage6.py
          python3 hotfix/apply_ggo_identity_minimap_stage7.py
          BASE=ga-build/client-ui/src/main/java/arena/client/shell
          test -f "$BASE/GgoShellScreen.java"
          test -f "$BASE/GgoShellHooks.java"
          test -f "$BASE/GgoCombatHud.java"
          test -f "$BASE/GgoFrontEndScreen.java"
          test -f "$BASE/GgoSettingsScreen.java"
          test -f "$BASE/GgoClientPreferences.java"
          test -f "$BASE/GgoAccountContext.java"
          grep -q 'ggo.account.authenticated' "$BASE/GgoAccountContext.java"
          grep -q 'minimapEnabled()' "$BASE/GgoCombatHud.java"
          ! grep -R 'new net.minecraft.client.gui.screens.OptionsScreen' "$BASE"

      - name: Bootstrap ForgeGradle wrapper
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/forge-mdk /tmp/forge-mdk.zip
          curl -fL --retry 3 -o /tmp/forge-mdk.zip \
            https://maven.minecraftforge.net/net/minecraftforge/forge/1.20.1-47.4.10/forge-1.20.1-47.4.10-mdk.zip
          mkdir -p /tmp/forge-mdk
          unzip -q /tmp/forge-mdk.zip 'gradlew' 'gradlew.bat' 'gradle/*' -d /tmp/forge-mdk
          cp /tmp/forge-mdk/gradlew ga-build/client-ui/gradlew
          cp /tmp/forge-mdk/gradlew.bat ga-build/client-ui/gradlew.bat
          rm -rf ga-build/client-ui/gradle
          cp -a /tmp/forge-mdk/gradle ga-build/client-ui/gradle
          chmod +x ga-build/client-ui/gradlew

      - name: Compile patched client UI
        working-directory: ga-build/client-ui
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon compileJava --stacktrace

      - name: Verify final GGO classes compiled
        shell: bash
        run: |
          set -euo pipefail
          BASE=ga-build/client-ui/build/classes/java/main/arena/client/shell
          for f in GgoShellScreen GgoShellHooks GgoCombatHud GgoFrontEndScreen GgoSettingsScreen GgoClientPreferences GgoAccountContext; do
            test -f "$BASE/$f.class"
          done
          echo 'GGO UI Shell Stage 1-7 compile gate passed'
